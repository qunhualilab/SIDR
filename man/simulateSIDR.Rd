% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulateSIDR.R
\name{simulateSIDR}
\alias{simulateSIDR}
\title{Simulate stratified observations under the SIDR model}
\usage{
simulateSIDR(n, ns, ind, mixps, mus, sigma, rho, omega)
}
\arguments{
\item{n}{Integer. Total number of observations to simulate.}

\item{ns}{Integer. Number of strata.}

\item{ind}{A list of length \code{ns}, where each element contains indices
corresponding to one stratum.}

\item{mixps}{Numeric vector of length \code{ns}, mixture proportions of the
reproducible component (\eqn{\pi_k}).}

\item{mus}{Numeric vector of length \code{ns}, means of the reproducible
component (\eqn{\mu_k}).}

\item{sigma}{Numeric. Standard deviation of the reproducible
component (\eqn{\sigma}).}

\item{rho}{Numeric. Correlation of the irreproducible component.}

\item{omega}{Numeric vector of length \code{ns}, correlation scaling
parameters (\eqn{\omega_k}) for the reproducible component.}
}
\value{
A data frame with \code{n} rows and three columns:
\describe{
\item{obs1}{Simulated observation from replicate 1.}
\item{obs2}{Simulated observation from replicate 2.}
\item{genuine}{Binary indicator (1 = reproducible / genuine signal, 0 = noise).}
}
}
\description{
This function simulates paired observations under a stratified Gaussian
mixture copula model.Each stratum has its own reproducible mixture proportion,
mean, and correlation scaling.
}
\examples{
set.seed(456)
n <- 10000
ns <- 3
x <- rexp(n, 1/4)
indx <-  1 * (x < 1.62) + 2 * (1.62 <= x & x < 3.89) + 3 * (x >= 3.89)
ind <- NULL
for (k in seq_len(ns)){
  ind[[k]] <- (1:n)[indx == k]
}
mixps <- c(0.8, 0.4, 0.2)
mus <- c(1.1, 1.0, 0.9)
sigma <- 1
rho <- 0.2
omega <- c(1.16, 0.75, 0.56)
sim_data <- simulateSIDR(n, ns, ind, mixps, mus, sigma, rho, omega)
thet.ini <- c(logit(mixps), mus, sigma, logit(rho), omega)
result <- fit_IDR_stratified(sim_data, ns, ind, thet.ini)

}
